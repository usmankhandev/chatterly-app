trigger:
- main
- development

pr:
- main
- development

pool:
  name: Default

variables:
- group: chatterly-secrets-ec2-8gb
- name: DOCKER_USERNAME
  value: 'osmankhandev'
- name: IMAGE_NAME
  value: 'chatterly-api'
- name: API_IMAGE
  value: '$(DOCKER_USERNAME)/$(IMAGE_NAME)'

stages:

- stage: Build
  jobs:
  - job: BuildAPIProject
    displayName: 'Build Chatterlys API Project'

    steps:

    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
    - script: npm ci
      displayName: 'Install Dependencies'

    - script: npx prisma generate --schema=apps/api/prisma/schema.prisma
      displayName: 'Prisma Generate'

    - script: npx nx build api
      displayName: 'Build Affected Apps Like API In this Time'

    - script: |
        docker build -t $(DOCKER_USERNAME)/$(IMAGE_NAME):$(Build.BuildId) -f apps/api/Dockerfile .
      displayName: 'Docker Build'

    - script: |
        docker tag $(DOCKER_USERNAME)/$(IMAGE_NAME):$(Build.BuildId) $(DOCKER_USERNAME)/$(IMAGE_NAME):latest
      displayName: 'Tag Docker Image'

    - task: AzureCLI@2
      displayName: 'Build and Push to ACR'
      inputs:
        azureSubscription: 'Azure Hardcore'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          echo "=== Login to ACR ==="
          az acr login --name acrchatterlydev

          echo "=== Build image ==="
          docker build \
            -t acrchatterlydev.azurecr.io/chatterly-api:$(Build.BuildId) \
            -t acrchatterlydev.azurecr.io/chatterly-api:latest \
            -f apps/api/Dockerfile .

          echo "=== Push to ACR ==="
          docker push acrchatterlydev.azurecr.io/chatterly-api:$(Build.BuildId)
          docker push acrchatterlydev.azurecr.io/chatterly-api:latest

- stage: DeployToAKS
  displayName: 'Deploy to AKS'
  dependsOn: Build
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/development'))
  jobs:
  - deployment: DeployToAKS
    displayName: 'Deploy API to AKS'
    environment: 'production'
    strategy:
      runOnce:
        deploy:
          steps:

          # ── Step 1: Login to Azure + get AKS credentials ──────────
          - task: AzureCLI@2
            displayName: 'Azure Login + Get AKS Credentials'
            inputs:
              azureSubscription: 'Azure Hardcore'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                echo "=== Getting AKS credentials ==="
                az aks get-credentials \
                  --resource-group rg-chatterly-dev \
                  --name aks-chatterly-dev \
                  --overwrite-existing

                echo "=== Verifying cluster connection ==="
                kubectl get nodes

          # ── Step 2: Update image tag to match built image ─────────
          - script: |
              echo "=== Updating image tag to $(Build.BuildId) ==="
              sed -i "s|acrchatterlydev.azurecr.io/chatterly-api:latest|acrchatterlydev.azurecr.io/chatterly-api:$(Build.BuildId)|g" \
                $(Build.SourcesDirectory)/k8s/base/api/deployment.yaml

              echo "=== Verifying image tag update ==="
              grep "image:" $(Build.SourcesDirectory)/k8s/base/api/deployment.yaml
            displayName: 'Update Image Tag'

          # ── Step 3: Apply manifests in correct order ──────────────
          - script: |
              echo "=== Applying namespace ==="
              kubectl apply -f $(Build.SourcesDirectory)/k8s/base/namespace.yaml

              echo "=== Applying config ==="
              kubectl apply -f $(Build.SourcesDirectory)/k8s/base/configmap.yaml
              kubectl apply -f $(Build.SourcesDirectory)/k8s/base/secret.yaml

              echo "=== Applying databases ==="
              kubectl apply -f $(Build.SourcesDirectory)/k8s/base/postgres/
              kubectl apply -f $(Build.SourcesDirectory)/k8s/base/redis/

              echo "=== Applying API ==="
              kubectl apply -f $(Build.SourcesDirectory)/k8s/base/api/
            displayName: 'Apply K8s Manifests'

          # ── Step 4: Wait for rollout to complete ──────────────────
          - script: |
              echo "=== Waiting for API rollout ==="
              kubectl rollout status deployment/api \
                -n chatterly-dev \
                --timeout=300s
            displayName: 'Wait for Rollout'

          # ── Step 5: Verify everything is running ──────────────────
          - script: |
              echo "=== Pod status ==="
              kubectl get pods -n chatterly-dev

              echo "=== Services ==="
              kubectl get svc -n chatterly-dev

              echo "=== API public IP ==="
              kubectl get svc api-svc -n chatterly-dev
            displayName: 'Verify Deployment'

          # ── Step 6: Cleanup kubeconfig ────────────────────────────
          - script: |
              rm -f ~/.kube/config
              echo "Kubeconfig cleaned"
            displayName: 'Cleanup'
            condition: always()
