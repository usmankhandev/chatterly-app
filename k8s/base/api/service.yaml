apiVersion: v1
kind: Service
metadata:
  name: api-svc
  namespace: chatterly-dev
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-sku: standard
spec:
  selector:
    app: api
  ports:
  - port: 80
    name: http
    targetPort: 3001
  type: ClusterIP
